# Shell scripts must stay LF — they run inside the Linux container and via
# `env bash`, where a CRLF shebang breaks with `env: 'bash\r': No such file`.
*.sh text eol=lf
entrypoint.sh text eol=lf

# Windows-only launcher stays CRLF
*.ps1 text eol=crlf
